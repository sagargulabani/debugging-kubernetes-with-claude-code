apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: demo
data:
  DATABASE_URL: "postgresql://demo-user:demo-pass@demo-db.internal:5432/demo"
  LOG_LEVEL: "INFO"
  # NOTE: API_KEY is intentionally missing. This is the bug for scenario 2.
